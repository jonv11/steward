namespace Steward.Core;

public enum OutputFormat
{
    Text,
    Json
}

public enum Verbosity
{
    Quiet,
    Normal,
    Verbose,
    Debug
}

public enum JsonEnvelopeMode
{
    Legacy,
    Standard
}
