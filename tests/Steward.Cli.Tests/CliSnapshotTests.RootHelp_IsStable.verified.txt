Description:
  Repository Steward — a configurable repository stewardship CLI for humans and AI agents

Usage:
  steward [command] [options]

Options:
  -o, --output <Json|Sarif|Text>                Output format: text or json; sarif is supported by check only [default: Text]
  -v, --verbosity <Debug|Normal|Quiet|Verbose>  Verbosity level: quiet, normal, verbose, or debug [default: Normal]
  --no-color                                    Disable colored output
  -c, --config <path>                           Path to .steward configuration directory
  -?, -h, --help                                Show help and usage information
  --version                                     Show version information

Commands:
  version            Print version and runtime information
  orient             Show repository structure classified by artifact role (readme, governance, planning, etc.)
  outline <path>     Show the repository file tree [default: .]
  init               Initialize .steward configuration
  config             Inspect and validate .steward configuration
  check              Validate repository against policy: required artifacts, frontmatter, links, naming, freshness
  md                 Markdown structural operations: query headings/frontmatter, inspect outlines, and edit documents safely
  search <query>     Search repository content and headings
  maintain           Deterministic maintenance of governed artifacts
  status             Show current repository state at a glance
  explain <rule-id>  Explain a validation rule, or use 'explain path <file>' to show effective governance for a file. Run without arguments to list all rules.
  refs <path>        Show inbound and outbound references for a file
  refactor           Refactoring operations for governed files
